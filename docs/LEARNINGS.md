# LEARNINGS — memoria del estudio CA6SNT (compounding)

**Qué es.** Memoria viva de aprendizajes. **Todo agente la lee al arrancar.** Cada proyecto que termina **añade** aquí lo que costó descubrir, para que el siguiente sea más fácil. No es bitácora de tareas: es conocimiento reutilizable.

> Origen: compound-engineering (`/compound`) + ReflexionMemory (SuperClaude) + retro (gstack).
> **Regla:** al cerrar cada proyecto, el orquestador hace una mini-retro y añade entradas. Si un agente repite un error que ya está aquí, es defecto.

---

## Cómo añadir una entrada
`[AAAA-MM-DD · proyecto · categoría] Síntoma → causa raíz → regla para la próxima.`
Mantenlo corto y accionable. Borra lo que quede obsoleto.

---

## Entorno / build
- `[2026-06-04 · WildRiftTracker · entorno]` Electron + módulos nativos (better-sqlite3) **NO compilan en Node 24** (Node 24 se compila con ClangCL y exige ese toolset). Síntoma: `MSB8020 ClangCL`. → **Regla: pin de runtime POR-PROYECTO con Volta** — campo `volta.node` en package.json (p. ej. `"22.11.0"`) **+ `engines` (`">=22 <23"`) + guard `preinstall`** que aborta si Node no es 22.x con un mensaje legible (mejor que el ClangCL críptico). **Reemplaza el enfoque de nvm** (que cambia el Node global y rompe otras apps): Volta fija el Node al entrar a la carpeta, sin `nvm use`, dejando el resto del sistema intacto.
- `[2026-06-04 · WildRiftTracker · entorno]` `node_modules` dentro de carpeta **OneDrive** corrompe instalaciones (electron `path.txt`, JSON a medio escribir). → **Regla: el repo de desarrollo vive FUERA de carpetas sincronizadas**; la nube respalda solo datos/outputs, nunca el árbol de build.
- `[2026-06-04 · WildRiftTracker · build]` `npm install`/`rebuild` NO compilan el código de la app: el proceso main TS necesita su propio build (`build:node`). → **Regla: el Architect/Backend exponen UN comando `build:all`** y el README lista el orden exacto (install→rebuild→build→build:node→start).

## Producto / honestidad
- `[2026-06-04 · WildRiftTracker · calidad]` Frontend rellenó datos faltantes con **placeholder/mock sin escalar** (iconos = iniciales porque el catálogo no traía arte; OCR mock que "detecta" siempre lo mismo). El PO tuvo que cazarlo. → **Regla (P-11): placeholder declarado o no hay placeholder.** Si falta un dato/asset real, se **escala** (RC-08); un placeholder que aparenta ser real es un **defecto**. QA lo audita con `qa-eyes`.
- `[2026-06-04 · WildRiftTracker · datos]` Elegir una fuente de datos (catálogo `ry2x`) sin verificar que cubre TODO lo que el diseño pide (no tenía arte de campeón). → **Regla: al elegir fuente de datos, el Researcher/Architect listan qué campos cubre y cuáles NO contra lo que pide el BRANDBOOK.**

## Proceso / orquestación
- `[2026-06-04 · WildRiftTracker · proceso]` El orquestador afirmó un dato de una imagen sin leerla bien (nombre de invocador ≠ campeón). → **Regla: no afirmar lo que no se verificó; en imágenes, leer el dato concreto, no inferir del contexto.**
- `[2026-06-04 · WildRiftTracker · integración]` Frontend y Backend en paralelo convergieron sin choque porque el **contrato (`shared/types.ts`) se fijó primero**. → **Regla: en trabajo paralelo, el contrato de integración es el primer entregable y su dueño está declarado.**
- `[2026-06-05 · Panorama · verificación]` El mount de shell del PO y la lectura de archivo de Claude Code discreparon sobre `TOKENS.css`; por seguir el mount casi se reescribe un archivo que en disco **ya estaba correcto** (tokens presentes). → **Regla: ante discrepancia mount-vs-lectura, la verdad es el DISCO REAL que ve Claude Code.** Verificar con Read/Select-String del archivo real antes de "arreglar"; no actuar sobre el mount (extiende "no afirmar lo que no se verificó").
- `[2026-06-05 · Panorama · research]` Un requisito de hardware crítico —el IC-7300 del PO **fijo en 19200** mientras el scope de banda exige 115200— apareció **tarde (gate 04)** e invalidó el producto (panadapter), forzando el pivote a "cabina". → **Regla: el Researcher (02) verifica de inmediato los requisitos/limitaciones de hardware contra la config REAL del PO** (baud, firmware, cableado, capacidades) antes de diseñar sobre supuestos; un supuesto de hardware no confirmado es riesgo de pivote.

## Estudio / infraestructura
- `[2026-06-04 · estudio · infraestructura]` El estudio está duplicado en .claude (Claude Code) y .codex (Codex); un cambio entró solo en .codex y divergieron. → **Regla: TODO cambio del estudio se aplica a AMBOS hogares; STUDIO.md lo declara.**
- `[2026-06-04 · estudio · docs]` STUDIO.md apuntaba solo a .codex pese al espejo en .claude, y se propagó a 6 handoffs. → **Regla: las rutas del estudio nombran ambos hogares; al escribir un handoff, verificar la ruta contra la realidad, no contra un doc desactualizado.**
- `[2026-06-04 · estudio · arquitectura]` Requisito del gate del Architect (04): **todo proyecto fija su runtime POR-PROYECTO desde el inicio (Volta)**, no se depende del Node global del sistema. → Verificación: un shell nuevo dentro de la carpeta da el `node -v` correcto SOLO por Volta (sin `nvm use`); fuera de la carpeta el sistema queda intacto.
- `[2026-06-05 · estudio · assets]` El placeholder de iconos se evita con **better-icons** (UI) + **fuente real de dominio (Data Dragon)** cacheada en build. → **Regla anti-iniciales = P-11.** (skill `asset-sourcing`.)
- `[2026-06-05 · estudio · entorno]` Para multi-lenguaje, **`mise` reemplaza a Volta** como pin por-proyecto; **Volta sigue válido para solo-Node**. → **Regla:** el Architect fija el runtime por-proyecto con mise (poliglota) o Volta (Node), siempre con `engines` + guard `preinstall`. (skill `env-reproducible`.)
- `[2026-06-05 · estudio · orquestación]` Bridge **despacho→aviso = claude-heartbeat + AMQ** (file-based, sin `claude -p`). → **Regla:** el orquestador lee el outbox del disco y reacciona; una tarea programada lo despierta a poll. **La aprobación de gate sigue siendo del PO** (P-05). (skill `orchestrator-bridge`.)
- `[2026-06-05 · Panorama · proceso]` Se dudó si RC-05 impedía al UX/UI **añadir** tokens a su propio `TOKENS.css` ya aprobado. → **Regla: RC-05 protege los archivos aprobados de cambios de OTROS agentes; el agente DUEÑO sí puede EXTENDER su propio entregable cuando el handoff lo autoriza (extender ≠ reescribir).** No se tocan entregables aprobados ajenos; el propio se amplía sin reabrir lo aprobado.
- `[2026-06-06 · Pizarra · hooks]` Se intentó imponer el **mapa de propiedad por-agente** con hooks `PreToolUse` (frontmatter + fallback). **No es posible hoy:** el `PreToolUse` **no expone la identidad del subagente llamante** ([#54898](https://github.com/anthropics/claude-code/issues/54898), abierto) y un `deny` es de **sesión** (afecta a todos los subagentes por igual, no solo al que no es dueño). → **Regla: el mapa de propiedad es NORMA + auditoría POST-HOC por observabilidad** (tool-spans con `file_path`+identidad real en Jaeger; ver `observability/OWNERSHIP_AUDIT.md`). Los `.mjs` por-identidad quedan **desconectados** (conservados en disco) hasta que #54898 se arregle. Enforcement mecánico real = **guards de sesión** (`session-safety-guard`) + **`approved_by:"PO"` en `GATE·`** (`taskcompleted-gate`).
- `[2026-06-06 · Pizarra · hooks]` `lead-guard.mjs` (R3) **congela el dossier de coordinación del LEAD** cuando hay **equipo persistente activo** (`~/.claude/teams/<t>/config.json`): bloquea su Write/Edit/Bash. En Pizarra no se materializó equipo real → quedó *fail-open* (no congeló), pero la **raíz es real**. → **Regla: antes de usar equipos persistentes, excepción explícita en `lead-guard` para el archivo de coordinación** (o el lead delega toda escritura). En v4.3 `lead-guard` se **desconectó** por la misma raíz (identidad de sesión, #54898).
- `[2026-06-06 · Pizarra · calidad]` Un **pre-gate de evals** (promptfoo + juez `claude-cli` *keyless*) cazó un **defecto funcional real** —`@media` dentro de una lista de selectores en `TOKENS.css`, CSS inválido que **desactivaba el dark automático**— que había **pasado el gate humano G03 Y los 49/49 unit tests** (los tests no miran CSS computado; el ojo no probó el modo auto). → **Regla: los evals ven lo que el ojo y los tests no.** 08 QA corre una rúbrica máquina sobre los entregables **antes** del gate humano (`evals/*.rubric.yaml`). Bonus verificado: el juez **ciego** (sin la autoevaluación dentro del archivo) replicó el **8.7** humano → **no se ancla**.
- `[2026-06-06 · RadioLogVivo · bootstrap]` Proyecto **greenfield**: `studio-init` activó el estudio en `D:\RadioLogVivo` pero la sesión seguía rooteada en `D:\` → los hooks y el `CLAUDE.md` del proyecto **no cargaron** (cargan solo al ABRIR la sesión rooteada en la carpeta; **no se recargan en caliente**). Huevo-y-gallina: la carpeta y su `settings.json` **no existen hasta el init**, así que la sesión que inicializa nunca está ya rooteada. → **Regla: tras `studio-init`, abrir una sesión NUEVA rooteada en el proyecto es OBLIGATORIO** (no "si hace falta") antes de materializar TaskGraph o spawnear. `/startsnt` quedó **idempotente / dos fases**: si ya rooteado + `settings.json` existe → continúa; si no → init y DETENERSE en el PASO 0.5. `studio-init` ahora imprime EN GRANDE ese siguiente paso cuando `cwd ≠ proyecto`.
- `[2026-06-06 · RadioLogVivo · proceso]` Confusión de arranque: se asumió **terminal** (`cd && claude`) cuando el PO usa la **app** (pestaña Code). Reabrir como **resume** conservó el cwd viejo; además `claude` no estaba en PATH (global npm). → **Regla: en la app, "rootear" = SESIÓN NUEVA + selector de carpeta de arriba** (no un comando). El CLI (`cd && claude` *fresco*, no `--resume`; PATH `;$env:APPDATA\\npm`) es **vía alternativa / pie de página** — no asumir terminal por defecto.
- `[2026-06-06 · RadioLogVivo · seguridad]` `studio-init.mjs .` con la cwd en una **raíz de disco** habría sembrado `CLAUDE.md`/`.claude`/`evals` en **todo `D:\`** (lo evitó el LEAD usando ruta explícita). → **Regla: `studio-init` SIEMPRE con RUTA ABSOLUTA, nunca `'.'`; el script ahora RECHAZA un target que sea la raíz de un disco.** Y el **mantenimiento del estudio** (editar `~/.claude`) se hace desde una sesión rooteada en `~/.claude`, porque el `session-safety-guard` de los proyectos bloquea escrituras fuera del árbol del proyecto (RC-04).

- `[2026-06-06 · RadioLogVivo · hooks]` `taskcompleted-gate` exigía `metadata.approved_by:"PO"` para cerrar un `GATE·`, pero el payload de `TaskCompleted` del build actual **NO entrega la metadata** de la tarea (1ª corrida agent-teams real; Claude Code #27556 [metadata no persiste] + #21356 [TaskGet no la expone], ambos *not planned*). El **subject SÍ se entrega**. → **Regla/fix:** el gate se cierra por **marcador `[PO-OK]` en el subject** — el LEAD lo añade SOLO tras OK explícito del PO (`TaskUpdate {subject:"… [PO-OK]"}` y luego `completed`), con `approved_by` como fallback forward-compat. Parche en el hook global `~/.claude/hooks/taskcompleted-gate.mjs` (`.bak` guardado) → studio-wide. Resuelve en NEGATIVO el ítem abierto de RETRO ("confirmar el campo de metadata del payload TaskCompleted").
- `[2026-06-06 · RadioLogVivo · evals]` `architecture.rubric.yaml` cargaba el contrato con `types: file://../src/shared/types.ts` y promptfoo ERRABA: `(intermediate value) is not a function` en `renderPrompt`, antes del juez. → **Causa raíz:** promptfoo carga vars `file://` por EXTENSIÓN — `.md/.css/.txt`=texto, pero **`.ts/.js/.mjs`=importa el archivo como módulo y LLAMA a su función exportada** (que debe devolver `{ output }`); types.ts solo exporta tipos. → **Regla/fix:** para inyectar un archivo de CÓDIGO como TEXTO en una rúbrica, usar un loader `.mjs` (`export default () => ({ output: readFileSync(...) })`) y apuntar la var a `file://./<loader>.mjs`. Sembrado `read-contract.mjs` en `templates/studio-project/evals`. `anti-placeholder.rubric` tiene el mismo riesgo si se re-apunta `target` a `.ts/.js`. Verificado: architecture PASS 1.0, brandbook 0.91.
- `[2026-06-06 · RadioLogVivo · evals/entorno]` promptfoo 0.121.15 exige Node `^20.20.0 || >=22.22.0`; el pin típico 22.11.0 (better-sqlite3/Electron, evita ClangCL de Node 24) es **demasiado viejo** → el pre-gate de evals no corre. → **Regla:** el Architect pinea Node **22.x pero ≥22.22.0** (satisface módulos nativos Y el tooling de evals).


## Cierre RadioLogVivo (6SNT.LLFH v1) — mini-retro 2026-06-06

- `[2026-06-06 · RadioLogVivo · verificación]` La verificación EN DISCO del LEAD cazó un defecto REAL en casi CADA etapa que el autoreporte del agente daba por bueno: T03 (hex de 7 dígitos inválido en el light-mode de TOKENS.css), T06 (9 errores de typecheck del renderer), T08/QA (DEFECT-01: el regex de indicativos descartaba los de prefijo numérico 9V1YC/4X4DX — crítico para un DXer), T10 (README inventó `npm run test:e2e` y contradijo el piloto = CQ zones). → **Regla: el LEAD VERIFICA cada entregable en disco (typecheck/build/grep/eval/tests/Read), nunca cierra una tarea por el autoreporte del agente.** El autoreporte se equivoca sin malicia; el disco no miente.
- `[2026-06-06 · RadioLogVivo · integración]` 06∥07 en paralelo convergieron sin choque gracias al contrato `types.ts` CONGELADO; el checkpoint de integración del LEAD (typecheck combinado main+renderer contra el contrato) cazó los errores de consumo (exactOptionalPropertyTypes, rootDir del tsconfig, dep `maplibre-gl` faltante) antes de QA. → **Regla: tras trabajo paralelo, el LEAD corre el typecheck de AMBOS lados contra el contrato antes de avanzar; las deps que un lado necesita del otro las media el LEAD (no hay SendMessage entre teammates).**
- `[2026-06-06 · RadioLogVivo · resiliencia]` Un agente opus-tier (Security) cayó por 529 overload; re-spawn en sonnet (equivalente) completó la tarea sin perder el hilo. → **Regla: ante 529/overload, el LEAD re-spawnea al agente en un tier equivalente (opus→sonnet) en vez de bloquearse; el modelo del loop lo fija el PO.**
- `[2026-06-06 · RadioLogVivo · proceso]` El pre-gate de evals + QA cazaron lo que el ojo y los tests unitarios no: un CSS inválido (T03) y un regex que perdía indicativos (T08). → **Confirma la regla del estudio: la rúbrica máquina y la auditoría de QA corren ANTES del gate humano y ven defectos reales; valió el coste.**


- `[2026-06-06 · RadioLogVivo · aprovisionamiento]` Si 02 RESEARCH identifica una fuente PÚBLICA de datos o assets que el producto necesita —cualquier dataset, tabla/padrón de referencia, fuente tipográfica, ícono, modelo o archivo de lookup—, el estudio debe APROVISIONARLA con un script de fetch cableado a `build:all`; NUNCA entregarla como tarea manual al PO. El entorno del agente puede no poder descargar (límites de red), pero el estudio AUTORA la automatización aunque no la ejecute (el build del PO la corre). → **Regla: fuente externa que el producto necesita = script `fetch:assets` → cableado a `build:all` (idempotente, cache + verificación de integridad, P-06 error explícito, P-11 fallback DECLARADO nunca silencioso, atribución de licencia en un NOTICE), jamás un paso manual.** La skill `asset-sourcing` (que cubría iconos/arte de código) se EXTIENDE a CUALQUIER dato/asset externo, sea cual sea el dominio (P-10).


- `[2026-06-06 · RadioLogVivo · proceso]` "Terminado" incluye un artefacto que ARRANCA + un smoke automático; el estudio ANTICIPA los entregables obvios (assets, artefacto empaquetado, smoke) — no espera que el PO los pida. → **Regla: lo que es el output evidente de un rol se produce como parte de su "terminado" (use PROACTIVELY); el gate de release lo verifica mecánicamente.** (Evidencia: hubo que PEDIR el .exe; debió salir solo. Aplica a cualquier dominio/stack — P-10.)
- `[2026-06-06 · RadioLogVivo · calidad]` Un smoke pre-release (arrancar el binario + render esencial, headless, pass/fail) caza bugs de ARRANQUE/integración que los unit tests y el QA headless NO ven. → **Regla: todo producto con artefacto ejecutable lleva un smoke de arranque cableado al gate de release (skill `smoke-test`).** (Evidencia: "ventana negra" = handlers IPC registrados DESPUÉS de cargar el renderer; 72 unit tests verdes no lo vieron — solo el arranque real del binario.)
- `[2026-06-06 · estudio · anti-bloat]` Agregar gates/agentes a un pipeline gated AUMENTA la probabilidad de fracaso (McEntire 2026). → **Regla: el riesgo del estudio es el EXCESO de proceso, no la falta. Default al tamaño MÁS PEQUEÑO que encaje (gates proporcionales: TRIVIAL/ESTÁNDAR = subset, no las 6); preferir menos gates + handoffs DETERMINISTAS; LLM-juez solo en el pre-gate de calidad/release; no importar marketplaces enteros (solo prompts puntuales).**
- `[2026-06-06 · RadioLogVivo · arquitectura]` Si el producto consume un recurso del SO COMPARTIDO (puerto, serial, dispositivo, GPU), asumir dueño único es un error de diseño. → **Regla: la Research/Arquitectura ASUME otros consumidores y diseña para COEXISTIR (multicast / fan-out / detección de exclusividad / proxy), no bind único.** (Evidencia: se asumió bind único del UDP de JTDX y CAT exclusivo del IC-7300; JTDX ya los usaba → hubo que pasar a estrategia primario-UDP.)

- `[2026-06-07 · estudio · proceso]` **Revisión adversarial externa > auto-revisión.** La auto-revisión cae en "trampa de coherencia": el autor pule respuestas equivocadas. Una pasada de crítico independiente y fresco ANTES del gate caza puntos ciegos que el autor no ve. → **Regla: implementar la revisión adversarial como SKILL reusable invocado por el agente productor (02/08/09), NO como un agente nuevo ni un council multi-modelo (anti-bloat).** (Evidencia: retro de 3 builds + research 2026 — reflexion con retornos decrecientes; el único robo útil del "council".)

